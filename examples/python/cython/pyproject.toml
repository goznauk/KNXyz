[build-system]
requires = ["setuptools>=68", "wheel", "Cython>=3,<4"]
build-backend = "setuptools.build_meta"

[project]
name = "knxyz-cython-smoke"
version = "0.0.3"
description = "Cython cimport smoke for the KNXyz PyCapsule C API."
requires-python = ">=3.10"
license = { text = "MIT" }
